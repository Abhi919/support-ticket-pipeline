# Product steering

- Prefer incremental delivery: smallest change that satisfies acceptance criteria.
- User-visible behavior must match Jira acceptance criteria; call out gaps explicitly in `requirements.md`.
- If scope is ambiguous, document assumptions and mark them **NEEDS_PRODUCT_CONFIRMATION**.
