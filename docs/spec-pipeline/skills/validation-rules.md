# Skill — Validation rules

- Validate at system boundaries (REST DTOs, message consumers).
- Prefer standard annotations where the stack supports them; align with existing validation style in the repo.
